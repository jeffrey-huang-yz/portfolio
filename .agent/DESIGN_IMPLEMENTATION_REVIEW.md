# Design implementation review — 2026-09-05

Implemented locally: editorial typography and spacing, quieter panels and interactions, shorter legacy Profile copy, grouped skills, hero links, CMS-controlled gallery prominence/order, normalized filters, responsive images, refreshed real portfolio thumbnail, Lenis scrolling, and the full-height blurred mobile navigation. The earlier bottom progress-bar removal remains intact.

The follow-up CMS changes are complete: all three presentation options validate correctly; optional MP4/WebM uploads render with controls, poster, captions/transcript support and image fallback; project-level `galleryFit` defaults to **cover** independently of `galleryImage`. This supersedes the original plan's contain default. Existing explicit nested fit choices are retained for compatibility, with the new field taking precedence.

## Verification completed

- Skills index, 2026-09-10: replaced low-hierarchy inline lists with four ruled groups, serif headings, counts and aligned two-column skill names. All 19 skills retained (6/5/5/3); unknown names still have a More tools group. Desktop 1440 px retains timeline alongside; tablet 768 px uses two category columns and phone 390 px stacks categories. No horizontal overflow in checked layouts. 38 tests/10 suites, lint, build and whitespace checks pass. Main JS 97.29 kB gzip; CSS 10.32 kB. Screenshots: `previews/skills-index-desktop.png`, `previews/skills-index-mobile.png`. Existing tooling advisories remain.

- Interests rebus completed 2026-09-10: four original transparent photo cutouts plus the requested Pokémon logo replace the old labels. Mixed existing fonts scale from 32 to 72 px; cutouts occupy line-height slots with natural proportions and phrase wrapping. One complete accessible sentence, stable fallback words, reduced-motion static output and one entrance are implemented. The generated card cutout is retained as an unused source after the logo substitution. Assets and prompts are documented in `frontend_react/src/assets/interests/README.md` and `.agent/rebus-image-prompts.json`.
- Rebus checks: 38 tests/10 suites, lint, production build and whitespace check pass. Browser at 320/390/1440 px confirms no overflow, all five images loaded, the Pokémon logo beside “collecting,” and placement after the collapsed profile. Light/dark theme controls retain readable paper/ink colors; theme restored to auto and viewport override cleared. No browser errors. Screenshots: `previews/interests-rebus-desktop.png` and `previews/interests-rebus-mobile.png`. Reduced-motion and failed-image behavior were tested in React, not by changing OS settings. Main JS 97.2 kB gzip, CSS 10.25 kB; existing Browserslist/punycode notices remain. No new dependencies or CMS changes.

- Profile reading sequence, 2026-09-06 02:00 EDT: modeled on the supplied 10-second recording of LaunchDarkly's “Control every change in production” section and the referenced homepage. Added the requested headline, three distinct numbered shapes, all-expanded initial state, sequential detail collapse, reverse reopening, and keyboard-accessible step/title controls. Desktop checks at 1440 × 1000 confirmed active steps 1/2/3, then none; detail heights 184/184/184 → 0/184/184 → 0/0/184 → 0/0/0. Visible stage height contracts from 931 to 379 px, and Interests follows the compact stage. The fixed 1782 px track remains stable throughout; Work's direct anchor lands at 100.1 px.
- Profile validation: all 34 tests/eight suites, lint, production build and whitespace checks pass. Four new tests cover initial/partial/final/reverse sequence, stable track/disclosure state, step navigation and reduced-motion restoration. Mobile 390 × 844 animates without overflow; Tab/Enter activates step 2 and retains focus. At 390 × 500 the section uses static titles and fully readable content in normal flow. Physical touch was not tested. Screenshots: `.agent/previews/profile-stack-expanded.png`, `profile-stack-second.png`, `profile-stack-collapsed.png`, `profile-stack-mobile.png`. Final main JS 96.57 kB gzip, CSS 10.2 kB. No additional dependencies; existing Browserslist/Node punycode advisories remain. The temporary recording copy was removed before building.

- Hero opening, completed 2026-09-06: full-screen paper and staggered element entrance; scroll-linked contraction with uniform content scaling; bounded pin distance followed by normal document scrolling. Desktop opening, midpoint and landing verified at 1440 × 1000; mobile opening verified at 390 × 844; no overflow at 320/390/900/1440 widths. At 700 px scroll the stage stops translating at 650 px and its paper matches the card within 0.1 px. The project CTA arrives at Work with the expected 99.9 px offset and destination focus. The Canvas waypoint uses the hero's settled position. Explicit stacking and a non-empty initial transform resolved a blank opening observed during preview QA.
- Final hero checks: 30 tests across seven suites pass, including seven new opening/landing/overscroll/tall-card/reduced-motion cases; frontend build, lint and whitespace checks pass. A live media subscription responds to preference changes because the installed Framer Motion 10 hook only stores its initial value. Final main JS is 95.58 kB gzip and CSS 9.64 kB. No new animation dependency. Existing Browserslist and Node punycode advisories remain. Screenshots: `hero-opening-desktop.png`, `hero-opening-mobile.png`, `hero-collapse-midpoint.png`, `hero-landed-desktop.png` under `.agent/previews`. Physical touch/GPU behavior and print appearance were not visually tested; reduced-motion transitions were verified by unit tests rather than changing the host OS setting.

- Follow-up, 2026-09-05 19:08 EDT: removed the mobile menu's journal heading and name/location footer at the user's request. Browser check at 390 × 844 confirms only navigation and the right-aligned close control remain, with 28 px blur and initial close focus. Both existing Navbar tests, frontend build, lint and diff check pass; `menu-mobile.png` refreshed. Existing Browserslist and Node punycode advisories remain.

- Follow-up, 2026-09-05 18:32 EDT: tightened Projects heading spacing and added lowercase display styling for descriptions and controls. Verified at 1440 px (36 px top padding, 28 px heading gap) and 390 px (24 px/16 px), with unclipped letters and no overflow. Computed styles confirm all rendered descriptions, filter buttons and project links are lowercase. Build/lint/diff checks pass; screenshots refreshed. CSS is now 9.32 kB gzip.

- Follow-up, 2026-09-05 18:24 EDT: Projects heading changed to full-width uppercase League Gothic, matching the user's condensed poster reference. Font metrics determine its container-relative size. Text/container measurements agree within 0.04 px at 320, 390, 900 and 1440 px; no horizontal overflow. Fresh frontend build and lint pass with the existing Browserslist database advisory. The additional Latin font is 10,436 bytes, bringing total font files to 111.6 KiB; CSS is now 9.28 kB gzip. Evidence: `.agent/previews/projects-heading-desktop.png` and `.agent/previews/projects-heading-mobile.png`.

- Frontend: 23 passing tests across six suites, including the four existing Canvas geometry tests, normalization/order/lead/filter cases, media precedence, cache corruption and delayed refresh, Lenis/native/reduced-motion navigation, modal focus/locks, and video behavior/fallback.
- Sanity: production build passed. `scripts/check-gallery-schema.cjs` uses the installed Sanity 3.23.4 validators and passes Lead/Featured/Standard, invalid presentation rejection, both fit choices/default, and integer order constraints.
- Frontend production build passed; lint passed with zero source warnings; whitespace diff check passed.
- Browser widths: 320, 390, 530, 900, 901, 980, 1440 px. No document overflow or overflowing headings after fixing the tablet hero type sizing. Standard grid switches one/two/three columns; mobile menu switches at 900/901 px.
- Phone menu: 390 × 844 full-viewport dialog, 28 px blur, Newsreader type, initial close-button focus, navigation dismissal. Short landscape: 390 px panel height with 733 px internally scrollable content. Resizing to desktop removes the dialog and releases body overflow.
- Light/dark/auto cycling verified: navbar backgrounds remain rgba(250,250,247,.82) or rgba(8,8,8,.82).
- Work anchor settles at 99.8 px with a 100 px root offset and destination focus. React filter changes nine bundled projects to five matching projects while GitHired stays Lead. Actual candidates included lead 1200 px AVIF and featured 800 px AVIF; Fill frame rendered as cover.
- Mobile Contact: 24 px heading/introduction gap, stamp below the introduction, no horizontal overflow. No browser console errors were observed in the final preview.
- Read-only CMS report returned seven published works on 2026-09-05T16:29:27Z; GitHired is now explicitly Lead. No published video assets yet. No production data was changed by the implementation.

## Delivery and measured cost

Follow-up, 2026-09-07 01:57 EDT: removed profile row +/- buttons and manual title toggling. Static titles retain their dimensions; scroll animation and numbered navigation remain. Updated existing visibility checks and README; 36 tests, lint, build and whitespace checks pass. Browser confirms zero row buttons and three numbered controls with the animated scene enabled. Main JS 96.63 kB gzip; CSS 10.19 kB. This supersedes the earlier row-title disclosure-button behavior.

Follow-up, 2026-09-07: smoothed both collapse sequences with quintic easing, longer scroll intervals and continuous pin releases; synchronized profile height/position writes and avoided redundant state/DOM updates. Softened the hero entrance and navbar reveal; both hero CTA labels are lowercase. All 36 tests/nine suites, lint and production build pass. Main JS is 96.72 kB gzip and CSS 10.22 kB; no new dependency. Desktop 1280 × 720 checks confirmed hero landing, active profile indicators 1 → 2 → 3 → none, full collapse, and reverse reopening. Mobile 390 × 844 confirmed lowercase CTAs, partial collapse and the second active item without overflow. No browser console errors observed. Evidence: `previews/motion-hero-mobile.png` and `previews/motion-profile-collapsed.png`. Physical input/GPU smoothness remains a device check. Existing Browserslist and Node punycode advisories remain.

Tracked frontend and Studio build output was regenerated. Main frontend JS is 92.22 kB gzip (baseline 81.02 kB after progress-bar removal); CSS is 9.22 kB gzip (baseline 8.09 kB). The two weight-only Latin WOFF2 fonts total 103,796 bytes / 101.4 KiB, below the 150 KiB target. Canvas code and its DPR cap are unchanged; scroll smoothing does not introduce scroll-driven React renders.

Evidence: `previews/gallery-desktop.png`, `previews/menu-mobile.png`, `previews/contact-mobile.png`. The CMS guide is `backend_sanity/GALLERY_EDITING.md`; scripts include a read-only curation proposal and a real schema regression check. Root `AGENTS.md` documents containers and the installed-tooling fallback.

## Rollout checks still requiring a suitable environment/content

Docker is unavailable on this host, so container builds were documented but not executed. The existing Browserslist dataset notice and Node punycode tooling deprecation remain; no framework-wide upgrade was attempted.

Actual uploaded video decoding/caption delivery must be checked with the user's first browser-compatible video. Fixture tests cover player attributes, pausing and fallback, not real codec delivery. Physical touch/trackpad feel, the original Windows GPU/native-scrollbar flicker, Safari behavior, live OS reduced-motion switching, 200% browser zoom, blocked-font layout-shift traces and comparative scroll performance recordings were not verified in this pass. Do not claim that Lenis fixes the earlier GPU issue. These are release/device checks, not evidence of a known source failure.

No deployment, commit, push, or production content write was performed. Existing deployed Studio instances require their own schema rollout; local development Studio can pick up the source changes.
